[build]
  base    = "frontend"
  command = "npm cache clean --force && npm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "https://travel-app-doee.onrender.com/api"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
